{
  "name": "@chatbot/root",
  "version": "1.0.0",
  "description": "",
  "workspaces": [
    "./packages/*"
  ],
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "npm run build --workspaces",
    "lint:check": "npm run lint:check --workspaces",
    "lint:fix": "npm run lint:fix --workspaces",
    "frontend:dev": "npm run dev --workspace=@chatbot/frontend",
    "prepare": "husky install && npx husky set .husky/pre-commit \"npm run lint:check\""
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "husky": "^8.0.3"
  }
}
