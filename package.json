{
  "name": "@chatbot/root",
  "private": true,
  "version": "1.0.0",
  "description": "",
  "workspaces": [
    "./packages/*"
  ],
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "npm run build --workspaces",
    "lint:check": "npm run lint:check --workspaces",
    "lint:fix": "npm run lint:fix --workspaces",
    "frontend:dev": "npm run dev --workspace=@chatbot/frontend",
    "backend:dev": "npm run dev --workspace=@chatbot/backend",
    "docker:cli": "docker-compose -f docker-compose.yml exec chatbot ",
    "docker:prod:cli": "docker-compose -f docker-compose.yml - exec chatbot ",
    "docker:init": "docker-compose up -d"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "shelljs": "^0.8.5"
  }
}
